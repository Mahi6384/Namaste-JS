javascript engine creates window object
window is a global object that is created along with the global execution context when a javasctipt program is executed. It represents the broweser window and provides various properties and methods to interact with the browser.
just like the window, javascript engine also creates this keyword

### At the global level this === wincdow

### Clousers

function bind together its lexical environment is called clousure
